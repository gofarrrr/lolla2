ruff==0.6.8

# METIS V2 RAG Dependencies - Operation "Intelligent Ingestion & RAG"
# Phase 1: Core RAG pipeline infrastructure

# Vector Database
pymilvus>=2.3.0

# Memory Layer
zep-python>=2.0.0

# Embeddings
voyageai>=0.2.0

# Web Intelligence (Future phases)
# firecrawl-py>=0.0.8
# apify-client>=1.7.0

# Document Processing (Future phases)  
# tensorlake>=0.1.0

# Context Engineering & 12-Factor Dependencies (New)
tiktoken>=0.5.1  # Token counting for context compilation
structlog>=23.1.0  # Structured logging for 12-factor compliance
tenacity>=8.2.3  # Retry logic with exponential backoff

# API Dependencies
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
redis>=5.0.0
psutil>=5.9.0
supabase>=0.7.1
asyncpg>=0.27.0
pgvector>=0.2.0
networkx>=3.0
python-multipart>=0.0.5
